export type Auth = {
    email: string;
    first_name : string;
    last_name : string;
    password : string;
}

export type RegisterRequest = {
    email: string;
    first_name: string;
    last_name: string;
    password: string;
}